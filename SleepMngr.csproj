<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <AssemblyName>SleepMngr</AssemblyName>
    <ApplicationIcon>app.ico</ApplicationIcon>

    <!-- Keep stale/local test projects under SleepMngr.Tests from being globbed
         into the main WinForms project by the SDK default Compile items. -->
    <DefaultItemExcludes>$(DefaultItemExcludes);SleepMngr.Tests/**</DefaultItemExcludes>
    
    <!-- Single file publish settings - without runtime -->
    <PublishSingleFile>true</PublishSingleFile>
    <SelfContained>false</SelfContained>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Management" Version="8.0.0" />
  </ItemGroup>

</Project>
